This task exports json data from the c++ program, this program uses the boost libary which is 
inluded under the packages folder. 

1. Open Debug
2. Run PlayerClass(.exe)
3. The program then will export all data from the c++ file into a json file (JSONwrite.json)
